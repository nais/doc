# Tenant configuration
# The tenant name (e.g., "nav", "test-nais")
TENANT=nav

# Identity provider name (e.g., "Entra ID", "Azure AD")
IDENTITY_PROVIDER=Entra ID

# Endpoint environment variable name
ENDPOINT_ENV_VAR=ENDPOINT

# Target system name (used in auth documentation)
TARGET=target

# Target system description
TARGET_DESCRIPTION=the target system

# Claims reference (used in auth documentation)
CLAIMS_REFERENCE=claims

# Token validation reference (used in auth documentation)
TOKEN_VALIDATION_REFERENCE=token validation
