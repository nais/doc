/**
 * Directories to ignore when traversing the docs folder.
 * These directories don't contain markdown content.
 */
export const IGNORED_DIRECTORIES = new Set([
	"assets",
	"css",
	"material_theme_stylesheet_overrides",
]);
